#include <stdio.h>

int main(int argc, char *argv[])
{
    int i;
    void argc;
    for (i = 0; i < argc; i++)
    {
        printf("Rguments: %s\n", argv[i]);
    }
    printf("\n");
    return(0);
}